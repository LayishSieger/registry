"use client"

import { DialRoot } from "dialkit"
import "dialkit/styles.css"

/** Docs-only live tuner. Hidden in production builds by DialKit default. */
export function ComposerDialRoot() {
  return <DialRoot />
}
